<?php
error_reporting(0);
class TopicController extends Base_Controller_Admin {
   // protected $needLogin = false;
  
     //初始化
     public function init(){ 
        parent::init();
     }
}
